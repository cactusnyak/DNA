Итак, заметил баг, связанный с счетчиком в кнопке, который появляется вместо кнопки добавления в корзину: счетчик

```
      <div className="flex items-center justify-center text-sm font-medium">
        {quantity}
      </div>
```

Не обновляется (всегда 0), при этом все остальное работает корректно (суммы, кружочки счетчкиков в корзине и тд), то есть не обновляется чисто значение `quantity`. Можешь проверить и поправить этот баг.