So, now general task: add general skeleton loader component which appiarance (make it like grid, or monolit) we can configurate via props. And after that we can use the loader in each places and adapted by potential view of replaced not yet loeaded elements (like normal skeleton loader). Make standart animation of gray gradient for skeleton loader elements (configurate it via `index.css`). don't forget use loader for item feeds (market/ads/mixed feed (imitate cards in a feed grid).