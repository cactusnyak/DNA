well, but why it's a different component for category/item image and for user? but separation is normal, but just make renaming of components: for user photo make separated component which will perform that it's exactly an avatar (rename the column too), but for categories and items use Admin table Image like current variant (for avatar we can create component like AdminTableAvatar, which will use current used `Avatar`).