Look at `web/src/widgets/Catalog/components/CatalogControls`. There is a complex logic of sticky behaviour for aside vesrion of catalog controls (`web/src/widgets/Catalog/components/CatalogControls/logic/use-sticky-panel.ts`). It works, but not so well as I want and there is a problem if the block has too big height (for example, if too many subcategories). How we can sole the problem? Can we just make the aside block just fixed and scrolled and linit it height as `100vh` (add scrool if it's hiest than 100vh).