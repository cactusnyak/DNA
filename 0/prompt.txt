Последний этап перед продом: давай сделаем описание продуктов и объявлений конфигурируемым markdown'ом. Сделаем пока базовую разметку, а сам ключ описания будет json'ом, а не строкой. Думаю, нам пока хватит заголовков и абзацев в качестве типов. Также давай сразу сделаем подходящий UI в админ-панели в формах создания/редактирования объявления/товара, пусть это будет `text-area` с классической системой разметки: `# <text>` — заголовок (на уровни не будем пока разделять), `<text>` — абзац (разделение строк через `\n` есть критерий отделения ключей в этом JSON). Таким образом нужно обновить и модель на бэке, и UI (админка и страница товара). НА странице товара должен просиходить рендеринг этого JSON-описания, но все описание должно быть в едином `section`-контейнере (сейчас описание — просто `<p>`-тег).