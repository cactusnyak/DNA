{
  "name": "dna",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d && npm run dev --prefix api & npm run dev --prefix web",
    "dev:api": "docker compose up -d && npm run start:dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:seed": "npm run seed:dev --prefix api",
    "db:seed:dev": "npm run seed:dev --prefix api",
    "db:seed:staging": "npm run seed:staging --prefix api",
    "db:seed:production": "npm run seed:production --prefix api",
    "db:migrate": "npx prisma migrate dev --prefix api",
    "install:all": "npm install --prefix api && npm install --prefix web"
  }
}
