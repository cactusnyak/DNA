# Default seed assets

Place immutable local source files for the default seed in this directory.

The current `seed-images.ts` scenario uses remote Unsplash sources, so this
directory intentionally contains no image binaries yet. Downloaded files are
runtime output and are written to `api/uploads/images`.
